# Stuff


Hello, it's me. math 6 x 45 = `r 6*45`. Did you know? maybe 8 * 9 = `julia 8*9`

```{.julia .numberLines startFrom="100"}

using Unitful      # use unitful.jl units package for Julia
using Plots

V₂ = V₁ * (P₁/P₂) ^ (1/n)    # from eq(1)
𝑣₁ = V₁ / m                  # from eq(2)
𝑣₂ = V₂ / m                  # from eq(2)
println("V₂ = ",V₂)
println("𝑣₁ = ",𝑣₁)
println("𝑣₂ = ",𝑣₂)
```

```sh
--template=mda.html5, --standalone, --pdf-engine=xelatex, --from=markdown+link_attributes+fenced_code_blocks+backtick_code_blocks+fenced_divs+fenced_code_attributes
```

